public partial class Const {

	public enum CameraState {
		ChaseTarget,
		Scroll,
		PlaceObstacle,
	}

	public enum StageState {
		Create,
		BraveManHomecoming,
	}

	public enum ObstacleState {
		Edit,
		Ready,
		Played,
	}

}